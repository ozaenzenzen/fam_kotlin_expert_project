<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FAMAndExpertApp1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FAMAndExpertApp1" parent="Base.Theme.FAMAndExpertApp1">
        <item name="android:actionBarStyle">@style/ToolbarTheme</item>
        Ï<item name="colorPrimary">@color/dark_blue_700</item>
        <item name="colorPrimaryVariant">@color/dark_blue_700</item>
        <item name="actionBarTheme">@style/ActionBarStyle</item>
<!--        <item name="android:tint">@color/white</item>-->
    </style>

    <style name="ActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="titleTextStyle">@style/ActionBarTitleText</item>
    </style>

    <style name="ActionBarTitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="colorControlNormal">@color/dark_blue_200</item> <!-- Color of the back button -->
        <item name="android:titleTextStyle">@style/Theme.MyAppTheme.ActionBar.TitleTextStyle</item>
    </style>

    <style name="Theme.MyAppTheme.ActionBar.TitleTextStyle" parent="android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#FFFFFF</item>
    </style>
</resources>